export function identity(request:Request){const header=request.headers.get('oai-authenticated-user-id');if(header)return {owner:header,cookie:''};const value=request.headers.get('cookie')?.match(/(?:^|;\s*)forge_learner=([a-f0-9-]{36})(?:;|$)/)?.[1];const id=value||crypto.randomUUID();return {owner:'guest:'+id,cookie:value?'':`forge_learner=${id}; HttpOnly; SameSite=Strict; Path=/; Max-Age=31536000${new URL(request.url).protocol==='https:'?'; Secure':''}`};}
